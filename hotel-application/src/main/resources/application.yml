spring:
  profiles:
    active: local

  application:
    name: hotel-application
  aop:
    auto: false
    proxy-target-class: false

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
jwt:
  secret: mysupersecretlongrandomkeymysupersecretlongrandomkey

cloudinary:
  


logging:
  level:
    root: INFO
    me.manishcodes.hotelapplication: DEBUG  # your main package
  file:
    name: logs/hotel-app.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"



